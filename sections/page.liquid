{{ 'page.css' | asset_url | stylesheet_tag: preload: true }}
{% render 'section-divider' %}

<div class="section-page relative mx-auto pt-[{{ section.settings.padding_top_mobile }}px] md:pt-[{{ section.settings.padding_top }}px] pb-[{{ section.settings.padding_bottom_mobile }}px] md:pb-[{{ section.settings.padding_bottom }}px] ltr">
  <div class="{% if section.settings.full_width %} full-width{% else %} page-width{% endif %}">
    <div class="page__container">
      {%- if section.settings.select_page.title != blank -%}
        <h2 class="h2 mb-5 pt-2 pb-2 text-left">{{ section.settings.select_page.title | escape }}</h2>
      {%- endif -%}
      <div class="page__body overflow-x-auto scrollbar-horizontal">
        <div class="rte">
          {%- if section.settings.select_page.content != blank -%}
            {{ section.settings.select_page.content }}
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</div>
{% schema %}
{
  "name": "t:sections.section-page.name",
  "tag": "section",
  "class": "section section-page x-section",
  "disabled_on": {
    "groups": [
      "aside"
    ]
  },
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.main-page.settings.description"
    },
    {
      "type": "page",
      "id": "select_page",
      "label": "t:sections.featured-product.blocks.size_chart.settings.select_page.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "default": false,
      "label": "t:sections.all.full_width.label"
    },
    {
      "type": "checkbox",
      "id": "show_section_divider",
      "default": false,
      "label": "t:sections.rich-text.settings.show_section_divider.label"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "unit": "px",
      "step": 4,
      "default": 28,
      "label": "t:sections.all.padding.padding_top"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "unit": "px",
      "step": 4,
      "default": 28,
      "label": "t:sections.all.padding.padding_bottom"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_layout_mobile"
    },
    {
      "id": "show_section_divider_mobile",
      "type": "checkbox",
      "default": false,
      "label": "t:sections.collage.settings.show_divider.label"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 100,
      "unit": "px",
      "step": 4,
      "default": 28,
      "label": "t:sections.all.padding.padding_top"
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 100,
      "unit": "px",
      "step": 4,
      "default": 28,
      "label": "t:sections.all.padding.padding_bottom"
    }
  ],
  "presets": [
    {
      "name": "t:sections.section-page.name"
    }
  ]
}
{% endschema %}